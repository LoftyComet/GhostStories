{
  "name": "ghost-house-agency-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 5173 --bind 127.0.0.1",
    "check": "find src -name '*.js' -exec node --check {} \\;"
  }
}
